# LLM API Key
# Get your API key from your chosen provider (e.g., OpenAI, Anthropic, Google)
# For OpenAI: https://platform.openai.com/api-keys
# For Anthropic: https://console.anthropic.com/
# For Google: https://aistudio.google.com/apikey
API_KEY=your-api-key-here

# LLM Model (e.g., "openai/gpt-4", "anthropic/claude-3-opus-20240229", "gemini/gemini-2.0-flash")
# See https://docs.litellm.ai/docs/providers for supported models
MODEL=gpt-4o-mini

# Optional: API Base URL (for custom endpoints or local models)
# API_BASE=https://api.openai.com/v1

# Default Command Lists (comma-separated)
# These are initial defaults; the agent will manage them dynamically via config/commands.json
# DEFAULT_ALLOWLIST=
# DEFAULT_BLACKLIST=rm,dd,format,mkfs,chmod 777

# Session Configuration
APP_NAME=flourish
USER_ID=user
SESSION_ID=session
